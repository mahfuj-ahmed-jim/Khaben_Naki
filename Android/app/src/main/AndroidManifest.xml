<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.khabennaki">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KhabenNaki"
        android:usesCleartextTraffic="true">
        <activity android:name=".Design.Home.HomeActivity" />
        <activity android:name=".Design.SignUp.All.PinCodeActivity" />
        <activity android:name=".Design.SignUp.Buyer.BuyerSignInActivity" />
        <activity android:name=".Design.SignUp.All.SelectActivity" />
        <activity android:name=".Design.SignUp.All.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>